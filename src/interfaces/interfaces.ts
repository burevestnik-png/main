export interface Project {
    name: string,
    readyProjectLink: string,
    sourceLink: string,
    description: string,
    picture: string
}

export interface ImageWrapper {
    image: any,
    name: string
}